services:
  server:
    build: ./server
    restart: unless-stopped
    volumes:
      - queue-data:/app/data

  client:
    build: ./client
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:80"
    depends_on:
      - server

volumes:
  queue-data:
